\chapter{Herramientas desarrolladas}\label{herramientas-desarrolladas}

Este capítulo describe las herramientas que, junto con el módulo de ingesta, constituyen el producto final del trabajo: la exportación a GeoTIFF (sección~\ref{exportacion-a-geotiff}), el motor de consultas analíticas (sección~\ref{motor-de-consultas-analiticas}), la comparativa frente a GeoTIFF (sección~\ref{benchmark-vs-geotiff}) y la aplicación web (sección~\ref{aplicacion-web-interactiva}). Las herramientas auxiliares de exploración y diagnóstico desarrolladas como apoyo (visualizador de escritorio y utilidades de consola) se describen en el \hyperref[anexo-e.-guia-de-despliegue]{Anexo~E}.

\section{Exportación a GeoTIFF}\label{exportacion-a-geotiff}

El sistema incluye exportación a GeoTIFF para interoperabilidad con herramientas SIG como QGIS, que no leen el formato TileDB: reconstruye el ráster denso a partir del array sparse (las celdas secas reciben \textit{nodata}), conservando EPSG:32614 y la resolución de 10 m, con posibilidad de restringirse a una subregión. Los metadatos geoespaciales se almacenan en el propio array durante la ingesta, por lo que no es necesario conservar los GeoTIFF originales en el servidor. Está disponible desde la aplicación web (botón de descarga por consulta) y de forma autónoma, concebida como herramienta puntual: exportar el escenario completo produciría $\sim$20 GB, anulando la ventaja de compresión.

\section{Motor de consultas analíticas}\label{motor-de-consultas-analiticas}

El motor de consultas analíticas es el núcleo del sistema. Proporciona 24 funciones Python que operan directamente sobre el array TileDB sparse sin exportación previa ni carga completa en memoria. Cada función acepta opcionalmente un parámetro bbox (ventana espacial en EPSG:32614) para restringir el análisis a una subregión.

Las consultas acumuladas por píxel (bloques C y G) emplean un \textit{grid} de tamaño fijo $n_r \times n_c$ que se actualiza paso a paso con \texttt{np.minimum.at}, \texttt{np.maximum.at} o \texttt{np.add.at}, según la consulta. Este enfoque mantiene el consumo de memoria constante ($\sim$750 MB sobre el dominio completo) independientemente del volumen de celdas húmedas procesado, frente a los más de 14 GB que requeriría acumular listas y concatenarlas al final. Las 24 funciones se organizan en ocho bloques (Tabla~\ref{tab:bloques-queries}); el catálogo completo con firma y parámetros se recoge en el \hyperref[anexo-a.-catalogo-completo-de-consultas-analiticas]{Anexo~A}.


\begin{center}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{3.2cm}>{\raggedright\arraybackslash}p{4.5cm}L}
\toprule
\textbf{Bloque} & \textbf{Funciones} & \textbf{Descripción} \\
\midrule
A. Puntuales & Q1, Q2, Q3 & Calado en punto, serie temporal y velocidad en zona \\
B. Umbrales & Q4/Q6, Q5 & Mapa de zonas inundadas con umbral predeterminado (Q4) o libre (Q6, variante parametrizada de Q4); evolución temporal de la extensión (Q5) \\
C. Temporales & Q7, Q8, Q9, Q11 & Hora de llegada del frente, duración de la inundación, hora de calado máximo y ventana de emergencia (intervalo con $H < 0{,}60$ m, límite de transitabilidad para vehículos de emergencia) \\
D. Russo et al.\ (2013) & Q10a, Q10b, Q10c, Q11b, Q17 & Peligro para adultos/niños/vehículos por umbrales de H y $Q_{mod}$ (Russo et al.); semáforo de 5 niveles con los cortes de 0,25 m y 0,50 m de Russo y tramos superiores propios de la aplicación \\
E. Xia et al.\ (2014/2022) & Q10a-Xia, Q10b-Xia, Q10c-Xia & Inestabilidad de adultos/niños/vehículos por velocidad crítica de inestabilidad ($U_{c,p}$, $U_{c,v}$); tres niveles de riesgo \\
F. Estadísticos & Q12, Q13, Q14, Q15 & Área, volumen, estadísticos de zona y semáforo de calado (3 niveles) \\
G. Evacuación & Q16 & Ventana temporal disponible para evacuación por píxel; valor $-$1 indica celdas que nunca alcanzan el umbral crítico en toda la simulación \\
H. Daños normativos & Q18 & Zona de graves daños según el Real Decreto 9/2008 (Reglamento del Dominio Público Hidráulico): celda afectada si cumple al menos una de $H > 1$ m, $V > 1$ m/s ó $H{\cdot}V > 0{,}5$ m$^2$/s \\
\bottomrule
\end{tabularx}
\captionsetup{hypcap=false}\captionof{table}{Organización de las 24 funciones analíticas por bloque.}
\label{tab:bloques-queries}
\end{center}

Los bloques D y E implementan criterios de peligrosidad complementarios: el bloque D aplica los umbrales fijos de calado y caudal unitario de Russo et al.\ \cite{russo2013}, mientras que el bloque E calcula para cada celda la velocidad crítica de inestabilidad de Xia et al.\ \cite{xia2014, xia2011vehicles} en función del calado local y de los parámetros antropométricos del sujeto (adulto, niño) o las dimensiones del vehículo. Los umbrales concretos y los parámetros de cada criterio se detallan en el \hyperref[anexo-a.-catalogo-completo-de-consultas-analiticas]{Anexo~A}.


\section{Comparativa de TileDB frente a GeoTIFF}\label{benchmark-vs-geotiff}

Para cuantificar la ventaja práctica del array TileDB sparse frente al acceso directo a los GeoTIFF originales ($\sim$235 GB) se ejecutó un \textit{benchmark} con seis casos de prueba que cubren los patrones de acceso más habituales en análisis hidráulico, siguiendo el protocolo de la sección~\ref{protocolo-de-medicion}. Los resultados sobre datos1 se recogen en la Tabla~\ref{tab:bench-datos1}.

\begin{center}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{3.0cm}RRRRRRR}
\toprule
\textbf{Caso}
  & \multicolumn{2}{c}{\textbf{TileDB (s)}}
  & \multicolumn{2}{c}{\textbf{GeoTIFF (s)}}
  & \textbf{Factor}
  & \multicolumn{2}{c}{\textbf{RAM (MB)}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}\cmidrule(lr){7-8}
  & $t_{\text{frío}}$ & $t_{\rm cal}$
  & $t_{\text{frío}}$ & $t_{\rm cal}$
  & & TDB & TIF \\
\midrule
C1. Instante completo   & 3,196 & \textbf{1,831}  & 16,598 & 22,757  & \textbf{12,4}$\times$ TDB & \textbf{2\,328} & 6\,012 \\
C2. Ventana 50$\times$50 km & 0,143 & \textbf{0,080} & 0,832  & 0,251  & \textbf{3,1}$\times$ TDB  & \textbf{158}    & 191 \\
C3. Serie temporal, 1 px & 0,086 & 0,067          & 0,056  & \textbf{0,013} & \textbf{5,3}$\times$ TIF & 90 & \textbf{$<$1} \\
C4. Peligrosidad (Russo)       & 43,797 & \textbf{41,421} & 270,690 & 293,211 & \textbf{7,1}$\times$ TDB & 3\,980 & \textbf{1\,055} \\
C5. Estadísticos zona   & 0,079 & \textbf{0,047}  & 0,630  & 0,114  & \textbf{2,4}$\times$ TDB  & \textbf{90}     & 191 \\
C6. Exportar ventana    & 0,393 & \textbf{0,296}  & 0,931  & 0,719  & \textbf{2,4}$\times$ TDB  & 230    & \textbf{191} \\
\bottomrule
\end{tabularx}
\captionsetup{hypcap=false}\captionof{table}{Benchmark TileDB vs GeoTIFF sobre datos1 (6 casos, 3 repeticiones en caché caliente). En negrita: mejor valor por fila.}
\label{tab:bench-datos1}
\end{center}

Factor = $t_{\rm cal}$(GeoTIFF) / $t_{\rm cal}$(TileDB) si gana TileDB, o $t_{\rm cal}$(TileDB) / $t_{\rm cal}$(GeoTIFF) si gana GeoTIFF.

TileDB supera a GeoTIFF en cinco de los seis casos. La ventaja más pronunciada se da en C1 (12,4$\times$): TileDB evita leer los píxeles secos ($\sim$92 \% del dominio), mientras que GeoTIFF debe descodificar los rásteres completos ($\approx$ 24 GB por instante). En C4 (peligrosidad sobre toda la serie temporal), la lectura densa de GeoTIFF agota la memoria si no se procesa por franjas, y aun así tarda 293 s frente a los 41 s de TileDB. El único caso favorable a GeoTIFF es C3 (serie temporal de un píxel, 5,3$\times$): rasterio resuelve una ventana 1$\times$1 con una lectura directa que el índice sparse de TileDB no puede igualar. En RAM, TileDB es más eficiente en las lecturas masivas (C1: 2\,328 frente a 6\,012 MB). En almacenamiento, la reducción es de $\sim$235 GB a 17\,611 MB (92,7 \%, ratio 13,7$\times$), debida principalmente al modelo sparse y en menor medida al filtro de compresión.

Para verificar la robustez, el \textit{benchmark} se repitió íntegramente sobre un segundo escenario (datos2) con el mismo patrón de victorias en los seis casos, y se calcularon las métricas principales de los cuatro \textit{datasets} de referencia (datos1--datos4), que confirman la coherencia del sistema entre escenarios de magnitud distinta. Las tablas completas de ambas validaciones se recogen en el \hyperref[anexo-b.-tablas-de-resultados-completas]{Anexo~B} (Tablas~\ref{tab:bench-datos2}, \ref{tab:comparativa-datos1-datos2} y~\ref{tab:metricas-datasets}). Posteriormente se procesaron los 52 escenarios restantes con el mismo \textit{pipeline} ETL: el conjunto completo de 56 \textit{datasets} comprende 40 episodios históricos (julio 1980 -- mayo 2019) y 16 de proyección climática futura (2020--2035) bajo el escenario de altas emisiones SSP5-8.5 (\textit{Shared Socioeconomic Pathway}~5).

La evolución horaria detallada (extensión e indicador de peligro para adultos en cada hora de simulación) se recoge en la Tabla~\ref{tab:evolucion-horaria} del \hyperref[anexo-b.-tablas-de-resultados-completas]{Anexo~B}.

\section{Aplicación web interactiva}\label{aplicacion-web-interactiva}

La aplicación web constituye la capa de presentación del sistema y el principal punto de acceso para el usuario final. Desarrollada con Streamlit \cite{streamlit}, expone las 24 consultas del motor analítico mediante una interfaz accesible desde el navegador sin instalación adicional de software ni escritura de código. La Figura~\ref{fig:app-streamlit} muestra la interfaz con un resultado real.

\begin{figure}[ht]
\centering
\includegraphics[width=\textwidth]{Imagenes/Captura_Spillway}
\caption{Interfaz de la aplicación Streamlit mostrando la consulta Q7 (hora de llegada del frente de inundación) sobre \texttt{datos29}.}
\label{fig:app-streamlit}
\end{figure}

Al arrancar, la aplicación descubre los \textit{datasets} disponibles y permite seleccionar el escenario activo desde la barra lateral, donde cada uno aparece con su alias y fecha de inicio (\textit{datos1 · Jul 1980}). Las 24 consultas se presentan en los ocho bloques del motor (A--H) como secciones desplegables, distinguiendo las consultas de respuesta inmediata de las que implican tiempos de cómputo elevados (ejecución manual y caché). Los resultados se muestran en tres formatos según la consulta: mapa interactivo, gráfico temporal o métricas numéricas; la Figura~\ref{fig:app-mapa-peligrosidad} muestra un resultado espacial. La selección de coordenadas admite clic sobre el mapa o introducción manual en UTM.

\begin{figure}[ht]
\centering
\includegraphics[width=\textwidth]{Imagenes/Captura_Q17}
\caption{Resultado espacial de Q17 (semáforo de peligrosidad Russo): mapa interactivo con las cinco categorías de riesgo y sus áreas en km².}
\label{fig:app-mapa-peligrosidad}
\end{figure}

\FloatBarrier
\medskip\noindent\textbf{Modo de comparación entre \textit{datasets}.}
La barra lateral permite activar un modo comparativo que ejecuta la misma consulta sobre un segundo \textit{dataset} con los mismos parámetros. La vista se divide en dos columnas (primario y secundario) con la métrica clave de cada escenario y el delta entre ambos; cuando el resultado es una serie temporal, las dos curvas se superponen en un único gráfico. El modo es compatible con todas las consultas del catálogo y constituye la herramienta principal para estudiar la variabilidad entre los 56 escenarios. El \hyperref[anexo-a.-catalogo-completo-de-consultas-analiticas]{Anexo~A} incluye un ejemplo de uso completo con capturas de ambas variantes (Figura~\ref{fig:app-comparacion}).

\medskip\noindent\textbf{Accesibilidad y despliegue.}
La aplicación soporta interfaz bilingüe (español e inglés), incorpora autenticación por usuario y contraseña para el despliegue en servidor remoto y permite exportar los resultados espaciales a GeoTIFF.

\medskip\noindent\textbf{Rendimiento y memoria.}
Las consultas de dominio completo requieren 30--44 s en la primera ejecución y se sirven desde caché en las siguientes; en despliegues con múltiples usuarios se aconseja al menos 16 GB de RAM en el servidor.

