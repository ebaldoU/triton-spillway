\chapter{Tecnología: TileDB}\label{tecnologia-tiledb}

Este capítulo describe el funcionamiento interno de TileDB: el modelo de datos sparse, la gestión de fragmentos y el mecanismo de compresión, con el objetivo de justificar las decisiones de diseño adoptadas en el capítulo siguiente.

\section{¿Qué es TileDB?}\label{que-es-tiledb}

TileDB es una base de datos orientada a arrays multidimensionales de código abierto. A diferencia de las bases de datos relacionales (tablas), TileDB organiza los datos en arrays N-dimensionales, lo que lo hace especialmente eficiente para datos científicos, rásteres geoespaciales y series temporales densas o dispersas.

TileDB no requiere servidor: los arrays se almacenan directamente en el sistema de ficheros local (o en la nube) como directorios con metadatos, fragmentos de datos y ficheros de índice. El acceso se realiza mediante la biblioteca Python (o C/C++/R) sin ningún servicio en ejecución. Esta arquitectura sin servidor reduce la complejidad operativa respecto a alternativas como SciDB o Rasdaman, que exigen un demonio en ejecución continua, lo que es especialmente relevante para despliegues en máquinas de uso compartido o con recursos limitados.

\section{Arrays sparse vs. densos}\label{arrays-sparse-vs.-densos}

TileDB soporta dos modos de almacenamiento:

\begin{itemize}
\item \textbf{Dense:} Almacena todos los valores del dominio, incluidos los nulos. Eficiente cuando el dominio está mayoritariamente relleno.
\item \textbf{Sparse:} Almacena únicamente las celdas no nulas junto con sus coordenadas. Eficiente cuando el dominio es mayoritariamente vacío.
\end{itemize}

En este proyecto se utiliza el modo disperso porque solo el $\sim$8,3 \% del dominio tiene agua. Almacenar el $\sim$92 \% restante (terreno seco) sería un desperdicio de espacio y tiempo de lectura. La Figura~\ref{fig:dense-vs-sparse} ilustra la diferencia entre ambos modos.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[x=0.6cm, y=0.6cm, font=\small\sffamily]
  %% --- Cuadrícula izquierda: almacenamiento denso (6 cols × 5 filas) ---
  \foreach \r in {0,...,4} {
    \foreach \c in {0,...,5} {
      \fill[gray!12] (\c,\r) rectangle +(1,1);
      \draw[gray!45, line width=0.4pt] (\c,\r) rectangle +(1,1);
      \node[gray!55, font=\tiny, inner sep=0pt] at (\c+0.5,\r+0.5) {0};
    }
  }
  % Celdas húmedas (8 de 30)
  \foreach \r/\c in {0/3, 1/2, 1/3, 2/1, 2/2, 2/3, 3/2, 4/4} {
    \fill[blue!28] (\c,\r) rectangle +(1,1);
    \draw[blue!60!black, line width=0.6pt] (\c,\r) rectangle +(1,1);
    \node[blue!75!black, font=\tiny\bfseries, inner sep=0pt] at (\c+0.5,\r+0.5) {H};
  }
  \node[font=\small\bfseries, align=center, above=3pt] at (3,5) {\textbf{Denso}\\[1pt]{\normalfont\scriptsize 30 celdas (dominio completo)}};
  \node[rotate=90, gray!55, font=\tiny] at (-0.7, 2.5) {filas};
  \node[gray!55, font=\tiny, below=3pt] at (3, 0) {columnas};

  %% --- Flecha de filtrado ---
  \draw[-Latex, thick, draw=blue!65!black] (6.4,2.5) -- (7.6,2.5)
    node[midway, above, font=\scriptsize\bfseries, blue!65!black] {H $\geq$ 0{,}01 m}
    node[midway, below, font=\tiny, blue!45!black] {filtro húmedo};

  %% --- Cuadrícula derecha: almacenamiento disperso (solo celdas húmedas) ---
  \def\dx{8}
  \draw[gray!28, dashed, line width=0.5pt] (\dx,0) rectangle +(6,5);
  \foreach \r in {1,...,4} { \draw[gray!18, very thin] (\dx,\r) -- (\dx+6,\r); }
  \foreach \c in {1,...,5} { \draw[gray!18, very thin] (\dx+\c,0) -- (\dx+\c,5); }
  \foreach \r/\c in {0/3, 1/2, 1/3, 2/1, 2/2, 2/3, 3/2, 4/4} {
    \fill[blue!28] (\dx+\c,\r) rectangle +(1,1);
    \draw[blue!60!black, line width=0.6pt] (\dx+\c,\r) rectangle +(1,1);
    \node[blue!75!black, font=\tiny\bfseries, inner sep=0pt] at (\dx+\c+0.5,\r+0.5) {H};
  }
  \node[font=\small\bfseries, align=center, above=3pt] at (\dx+3,5) {\textbf{Disperso}\\[1pt]{\normalfont\scriptsize 8 celdas ($\sim$27\,\%)}};
\end{tikzpicture}
\caption{Diferencia entre almacenamiento denso y disperso sobre un dominio de ejemplo de 6$\times$5 celdas. El modo denso almacena todas las celdas incluyendo las secas (valor cero, en gris). El modo disperso almacena únicamente las celdas húmedas (en azul) junto con sus coordenadas, eliminando el $\sim$73\,\% de entradas vacías en este ejemplo. En el dominio real del proyecto la fracción seca es del $\sim$92\,\%, lo que reduce los 235\,GB de GeoTIFF a 17,5\,GB en TileDB sparse.}
\label{fig:dense-vs-sparse}
\end{figure}

\section{Fragmentos y consolidación}\label{fragmentos-y-consolidacion}

Cada escritura en TileDB crea un fragmento independiente en disco. En este proyecto se realiza una escritura por paso temporal, lo que produce \textbf{20 fragmentos} (uno por cada intervalo de 6 horas). Mantenerlos separados tiene una ventaja importante: TileDB puede filtrar por la dimensión \texttt{time} leyendo solo los fragmentos relevantes, lo que acelera las consultas acotadas temporalmente.

La API de TileDB ofrece \texttt{tiledb.consolidate()} para fusionar todos los fragmentos en uno solo, y \texttt{tiledb.vacuum()} para eliminar los fragmentos anteriores. En este proyecto esta operación \textbf{no se aplica}: la consolidación agrupa celdas de distintos pasos temporales en los mismos tiles, lo que obliga a leer datos innecesarios en consultas que solo acceden a uno o pocos pasos (\textit{tile amplification}). Las pruebas realizadas durante el benchmark de configuración (véase \hyperref[anexo-b.-tablas-de-resultados-completas]{Anexo~B}, grupo G4) confirman que mantener un fragmento por paso temporal es la opción más eficiente para el filtrado por tiempo; la consolidación en un único fragmento penaliza las consultas multi-paso por \textit{tile amplification}.

\section{Compresión}\label{compresion}

Se utilizó el filtro ByteShuffle+ZSTD nivel 9 \cite{tiledb_compression_tutorial, tiledb_compression, tiledb_docs_compression}. Tras un \textit{benchmark} exhaustivo de 15 configuraciones (ZSTD puro niveles 1, 3, 5 y 9, ByteShuffle+ZSTD y BitShuffle+ZSTD en los mismos niveles, y DeltaFilter+ZSTD inaplicable en float32) se concluyó que las diferencias de velocidad de lectura entre configuraciones son menores que el ruido de la caché del sistema operativo. El criterio determinista es el tamaño en disco: ByteShuffle+ZSTD-9 ofrece el menor tamaño (860 MB/paso en la región de prueba) gracias al reordenamiento de bytes de los float32, que agrupa exponentes y mantisas similares antes de comprimir. Reduce el almacenamiento un 8,9 \% adicional respecto a ZSTD-1 sin penalización apreciable en lectura.

La sección~\ref{diseno-del-sistema} presenta el diseño del array TileDB y el proceso ETL de carga de datos. El \hyperref[anexo-d.-proceso-de-ingesta-etl-detallado]{Anexo~D} describe el proceso de ingesta con mayor nivel de detalle.

\chapter{Diseño del Sistema}\label{diseno-del-sistema}

El sistema está compuesto por cuatro capas articuladas en torno al array TileDB sparse como componente central de almacenamiento, tal y como muestra la Figura~\ref{fig:arquitectura}:

\begin{enumerate}
\item \textbf{Ingesta (ETL):} El módulo de ingesta lee los ficheros GeoTIFF generados por Triton, aplica el filtro de celdas húmedas (H $\geq$ 0,01 m) y escribe los datos en TileDB con filtro ByteShuffle+ZSTD-9. La lectura se realiza por franjas de 5 000 filas para limitar el uso de memoria.
\item \textbf{Almacenamiento:} Array TileDB sparse con dimensiones (time, row, col) y atributos (H, QX, QY, MH). Un fragmento por paso temporal facilita el filtrado temporal eficiente.
\item \textbf{Análisis:} El motor de consultas proporciona 20 funciones que operan directamente sobre el array TileDB sin exportación previa.
\item \textbf{Visualización y exportación:} Un visualizador interactivo, un exportador a GeoTIFF para interoperabilidad SIG y la aplicación web Streamlit con mapas interactivos, comparativa multi-escenario y panel de ayuda.
\end{enumerate}

\section{Arquitectura del array TileDB}\label{arquitectura-del-array-tiledb}

El array disperso se define con tres dimensiones y cuatro atributos, tal y como se muestra en la Tabla~\ref{tab:array-schema}:

\begin{table}[ht]
\centering
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{3cm}>{\raggedright\arraybackslash}p{2cm}L}
\toprule
\textbf{Elemento} & \textbf{Tipo} & \textbf{Detalles} \\
\midrule
Dimensión time & int32 & Índice del paso temporal (0--19) \\
Dimensión row & int32 & Fila del ráster (0--34\,199) \\
Dimensión col & int32 & Columna del ráster (0--23\,039) \\
\addlinespace[4pt]
Atributo H & float32 & Profundidad instantánea \\
Atributo QX & float32 & Caudal unitario X \\
Atributo QY & float32 & Caudal unitario Y \\
Atributo MH & float32 & Profundidad máxima histórica \\
\bottomrule
\end{tabularx}
\caption{Dimensiones y atributos del array TileDB sparse.}
\label{tab:array-schema}
\end{table}

La Figura~\ref{fig:tiledb-estructura} ilustra cómo TileDB organiza físicamente estos datos: 20 fragmentos independientes (uno por paso temporal) almacenan únicamente las coordenadas y valores de las celdas húmedas, con los cuatro atributos codificados juntos mediante ByteShuffle+ZSTD-9.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[x=1cm, y=1cm, font=\small\sffamily]

  %% ── Parámetros globales ──────────────────────────────────────────
  \def\W{5.0}    % ancho cara frontal (columnas)
  \def\H{3.2}    % alto cara frontal (filas)
  \def\ox{0.38}  % offset oblicuo X por fragmento
  \def\oy{0.28}  % offset oblicuo Y por fragmento
  \def\NV{7}     % fragmentos visibles en el stack

  %% ── Stack de fragmentos: de atrás a delante ─────────────────────
  \foreach \k in {6,5,4,3,2,1,0} {
    \pgfmathsetmacro\dx{\k*\ox}
    \pgfmathsetmacro\dy{\k*\oy}
    \pgfmathsetmacro\tcol{int(\k*4)}   %% t_0, t_4, t_8 ...
    % Cara superior
    \fill[blue!14, draw=blue!30, line width=0.35pt]
      (\dx,\dy+\H) -- (\dx+\ox,\dy+\H+\oy) --
      (\dx+\W+\ox,\dy+\H+\oy) -- (\dx+\W,\dy+\H) -- cycle;
    % Cara lateral derecha
    \fill[blue!22, draw=blue!30, line width=0.35pt]
      (\dx+\W,\dy) -- (\dx+\W+\ox,\dy+\oy) --
      (\dx+\W+\ox,\dy+\H+\oy) -- (\dx+\W,\dy+\H) -- cycle;
    % Cara frontal
    \fill[blue!8, draw=blue!38, line width=0.5pt]
      (\dx,\dy) rectangle (\dx+\W,\dy+\H);
    % Etiqueta de paso temporal
    \ifnum\k>0
      \node[blue!55!black, font=\tiny, anchor=west]
        at (\dx+\W+\ox+0.07, \dy+\H/2+\oy/2)
        {$t_{\tcol}$};
    \fi
  }

  %% ── Grid disperso en la cara frontal (fragmento t_0) ────────────
  % Mini-rejilla 10 x 7 representando el dominio espacial
  \def\cs{0.5}   % tamaño celda X
  \def\rs{0.457} % tamaño celda Y
  % Celdas secas (gris claro)
  \foreach \ci in {0,...,9} {
    \foreach \ri in {0,...,6} {
      \fill[gray!18] (\ci*\cs+0.03, \ri*\rs+0.03)
                     rectangle (\ci*\cs+\cs-0.03, \ri*\rs+\rs-0.03);
    }
  }
  % Celdas húmedas (~8 %): 6 de 70 ≈ 8.6 %
  \foreach \ci/\ri in {1/1, 2/3, 3/5, 5/2, 6/4, 7/1, 8/5, 4/3, 2/0, 9/3} {
    \fill[blue!60] (\ci*\cs+0.03, \ri*\rs+0.03)
                   rectangle (\ci*\cs+\cs-0.03, \ri*\rs+\rs-0.03);
  }
  % Etiqueta "t_0" sobre la cara frontal
  \node[blue!75!black, font=\scriptsize\bfseries] at (\W/2, -0.22) {$t_0$};

  %% ── "..." entre fragmentos visibles y t_19 ───────────────────────
  \pgfmathsetmacro\dxlast{(\NV+0.5)*\ox}
  \pgfmathsetmacro\dylast{(\NV+0.5)*\oy}
  \node[gray!55, font=\normalsize\bfseries]
    at (\W/2+\dxlast, \H/2+\dylast) {$\cdots$};
  \node[blue!50!black, font=\tiny, anchor=west]
    at (\W+(\NV+1)*\ox+0.08, \H/2+(\NV+1)*\oy/2)
    {$t_{19}$};

  %% ── Ejes de coordenadas ──────────────────────────────────────────
  % Eje col (→)
  \draw[-{latex}, gray!60, thick] (0,-0.45) -- (\W+0.25,-0.45);
  \node[gray!58, font=\scriptsize, below=1pt] at (\W/2,-0.45)
    {columna \quad (0\,–\,23\,039)};
  % Eje row (↑)
  \draw[-{latex}, gray!60, thick] (-0.4,0) -- (-0.4,\H+0.25);
  \node[gray!58, font=\scriptsize, rotate=90, above=2pt] at (-0.4,\H/2)
    {fila \quad (0\,–\,34\,199)};
  % Eje tiempo (oblicuo)
  \draw[-{latex}, blue!55!black, thick]
    (0,\H+0.15) -- ++({\NV*\ox+0.25},{\NV*\oy+0.25});
  \node[blue!60!black, font=\scriptsize, rotate=36, above=3pt]
    at ({\NV*\ox/2+0.2},\H+{\NV*\oy/2+0.3}) {tiempo (20 fragmentos)};

  %% ── Leyenda celdas ───────────────────────────────────────────────
  \fill[blue!60] (0.15,\H+0.95) rectangle (0.45,\H+1.22);
  \node[right, font=\tiny] at (0.5,\H+1.07) {celda húmeda ($\sim$8\,\% del dominio)};
  \fill[gray!18, draw=gray!40, line width=0.3pt]
    (3.4,\H+0.95) rectangle (3.7,\H+1.22);
  \node[right, font=\tiny] at (3.75,\H+1.07) {celda seca (no almacenada)};

  %% ── Cuadro de estructura por celda ──────────────────────────────
  \def\bx{6.5}
  \def\by{-0.1}
  \def\bw{4.0}

  % Cabecera
  \fill[blue!45!black, rounded corners=3pt]
    (\bx,\by+4.65) rectangle (\bx+\bw,\by+5.15);
  \node[white, font=\small\bfseries] at (\bx+\bw/2,\by+4.9)
    {Estructura de cada celda};

  % Marco
  \draw[blue!35!black, rounded corners=3pt, thick, fill=blue!2]
    (\bx,\by) rectangle (\bx+\bw,\by+4.65);

  % ── Sección coordenadas
  \node[blue!55!black, font=\tiny\bfseries, anchor=west]
    at (\bx+0.15,\by+4.38) {COORDENADAS};
  \draw[gray!25, line width=0.5pt]
    (\bx+0.1,\by+4.22) -- (\bx+\bw-0.1,\by+4.22);
  \foreach \lbl/\rng/\ypos in {
    time/{int32, rango 0\,–\,19}/3.95,
    row/{int32, rango 0\,–\,34\,199}/3.55,
    col/{int32, rango 0\,–\,23\,039}/3.15%
  }{
    \node[font=\tiny, anchor=west] at (\bx+0.2,\by+\ypos)
      {\texttt{\lbl}};
    \node[font=\tiny, gray!60, anchor=east] at (\bx+\bw-0.15,\by+\ypos)
      {\rng};
  }

  % Separador
  \draw[gray!30, line width=0.6pt]
    (\bx+0.1,\by+2.9) -- (\bx+\bw-0.1,\by+2.9);

  % ── Sección atributos
  \node[orange!65!black, font=\tiny\bfseries, anchor=west]
    at (\bx+0.15,\by+2.65) {ATRIBUTOS (float32)};
  \draw[gray!25, line width=0.5pt]
    (\bx+0.1,\by+2.48) -- (\bx+\bw-0.1,\by+2.48);
  \foreach \lbl/\desc/\ypos in {
    H/{Calado instantáneo (m)}/2.18,
    QX/{Caudal unitario X (m\textsuperscript{2}/s)}/1.75,
    QY/{Caudal unitario Y (m\textsuperscript{2}/s)}/1.32,
    MH/{Calado máximo histórico (m)}/0.89%
  }{
    \node[font=\tiny\bfseries, anchor=west] at (\bx+0.2,\by+\ypos)
      {\texttt{\lbl}};
    \node[font=\tiny, gray!55, anchor=west] at (\bx+0.75,\by+\ypos)
      {— \desc};
  }

  % ── Sección compresión y tamaño
  \draw[gray!30, line width=0.6pt]
    (\bx+0.1,\by+0.65) -- (\bx+\bw-0.1,\by+0.65);
  \node[orange!65!black, font=\tiny, align=center] at (\bx+\bw/2,\by+0.42)
    {Filtro: ByteShuffle + ZSTD-9};
  \node[gray!60, font=\tiny, align=center] at (\bx+\bw/2,\by+0.15)
    {$\sim$17,5 GB por escenario (20 pasos)};

  %% ── Flecha fragmento → estructura celda
  \draw[-{latex}, blue!40!black, thick, dashed]
    (\W+0.15, \H/2)
    to[out=0, in=180] (\bx-0.12, \by+2.5);

\end{tikzpicture}
\caption{Estructura interna del array TileDB sparse. \textbf{Izquierda:} los 20 pasos temporales se almacenan como 20 fragmentos independientes apilados en la dimensión temporal. La cara frontal del fragmento $t_0$ muestra el dominio espacial (fila $\times$ columna): las celdas húmedas (azul, $\sim$8\,\% del total) se almacenan con sus coordenadas; las celdas secas (gris) no se guardan. \textbf{Derecha:} cada celda húmeda almacena sus tres coordenadas (time, row, col) y cuatro atributos en float32 (H, QX, QY, MH), comprimidos con ByteShuffle+ZSTD-9. El resultado son $\sim$17,5 GB por escenario frente a los $\sim$235 GB de los GeoTIFF originales.}
\label{fig:tiledb-estructura}
\end{figure}

\begin{figure}[ht]
\centering
\begin{tikzpicture}[
    font=\small\sffamily,
    node distance=10mm,
    source/.style={rectangle, draw=black!70, fill=black!5, minimum height=11mm, minimum width=22mm, align=center, rounded corners=2pt},
    process/.style={rectangle, draw=blue!70!black, fill=blue!10, minimum height=11mm, minimum width=22mm, align=center, rounded corners=2pt, thick},
    storage/.style={cylinder, draw=orange!80!black, fill=orange!15, shape border rotate=90, aspect=0.25, minimum height=14mm, minimum width=26mm, align=center, thick},
    consumer/.style={rectangle, draw=green!50!black, fill=green!10, minimum height=10mm, minimum width=24mm, align=center, rounded corners=2pt},
    ui/.style={rectangle, draw=red!70!black, fill=red!8, minimum height=10mm, minimum width=28mm, align=center, rounded corners=2pt, thick},
    arrow/.style={-Latex, thick, draw=black!60},
]
    \node[source] (geo) {GeoTIFF Triton\\\scriptsize 1 escenario de simulación};
    \node[process, right=14mm of geo] (etl) {ETL\\\scriptsize filtrado y compresión};
    \node[storage, right=14mm of etl] (tdb) {TileDB sparse\\\scriptsize solo celdas húmedas};

    \node[consumer, below=14mm of tdb, xshift=-32mm] (queries) {Motor de consultas\\\scriptsize 20 funciones analíticas};
    \node[consumer, below=14mm of tdb, xshift=4mm] (viz) {Visualizador};
    \node[consumer, below=14mm of tdb, xshift=40mm] (exp) {Exportador GeoTIFF};

    \node[ui, below=12mm of viz] (app) {Aplicación web Streamlit};

    \draw[arrow] (geo) -- (etl);
    \draw[arrow] (etl) -- (tdb);
    \draw[arrow] (tdb.south) -- ++(0,-3mm) -| (queries.north);
    \draw[arrow] (tdb.south) -- ++(0,-3mm) -| (viz.north);
    \draw[arrow] (tdb.south) -- ++(0,-3mm) -| (exp.north);
    \draw[arrow] (queries.south) -- (app.north);
\end{tikzpicture}
\caption{Arquitectura del sistema. Los GeoTIFF generados por Triton se procesan mediante el ETL (filtrado con rasterio) y se almacenan en TileDB sparse. El motor de consultas analíticas expone los datos a la aplicación Streamlit y permite exportarlos de nuevo a GeoTIFF.}
\label{fig:arquitectura}
\end{figure}

\FloatBarrier
\section{Proceso de carga ETL (geotiff\_to\_tiledb\_sparse.py)}\label{proceso-de-carga-etl-geotiff_to_tiledb_sparse.py}

Se ha creado un script en Python para cargar todos los datos GeoTIFF a TileDB. La Figura~\ref{fig:etl-flow} describe el flujo completo; el cargador sigue estos pasos para cada paso temporal:

\begin{figure}[ht]
\centering
\begin{tikzpicture}[
    font=\small\sffamily,
    node distance=7mm,
    startstop/.style={rectangle, draw=black!70, fill=black!8, minimum height=8mm, minimum width=44mm, align=center, rounded corners=4pt},
    process/.style={rectangle, draw=blue!70!black, fill=blue!8, minimum height=9mm, minimum width=58mm, align=center, rounded corners=2pt, thick},
    decision/.style={diamond, draw=orange!80!black, fill=orange!10, aspect=2.4, minimum height=9mm, align=center, thick, inner sep=1pt},
    loop/.style={rectangle, draw=violet!70!black, fill=violet!8, minimum height=8mm, minimum width=58mm, align=center, dashed, thick},
    arrow/.style={-Latex, thick, draw=black!60},
]
    \node[startstop] (start) {Inicio (\ttfamily --dataset)};
    \node[loop, below=of start] (loop1) {\textbf{Bucle:} para cada paso temporal $t \in \{0, \ldots, 19\}$};
    \node[process, below=of loop1] (read) {Lectura paralela de 4 GeoTIFF (H, QX, QY, MH)\\\scriptsize ThreadPoolExecutor, ventanas de 5 000 filas};
    \node[process, below=of read] (filter) {Filtro de celdas húmedas\\\scriptsize H $\geq$ 0,01 m};
    \node[process, below=of filter] (accum) {Acumulación de coordenadas (row, col) y valores};
    \node[process, below=of accum] (write) {Escritura en TileDB sparse\\\scriptsize 1 fragmento por paso, filtro ByteShuffle + ZSTD-9};
    \node[decision, below=of write] (more) {¿quedan pasos?};
    \node[process, below=8mm of more] (verify) {Verificación de integridad\\\scriptsize muestreo aleatorio TileDB vs GeoTIFF};
    \node[startstop, below=of verify] (end) {Fin};

    \draw[arrow] (start) -- (loop1);
    \draw[arrow] (loop1) -- (read);
    \draw[arrow] (read) -- (filter);
    \draw[arrow] (filter) -- (accum);
    \draw[arrow] (accum) -- (write);
    \draw[arrow] (write) -- (more);
    \draw[arrow] (more.east) -- ++(20mm,0) |- node[pos=0.25, right, font=\scriptsize] {sí} (loop1.east);
    \draw[arrow] (more) -- node[right, font=\scriptsize] {no} (verify);
    \draw[arrow] (verify) -- (end);
\end{tikzpicture}
\caption{Diagrama de flujo del proceso ETL. Por cada escenario se iteran los 20 pasos temporales: se leen los cuatro ficheros GeoTIFF, se filtra por H $\geq$ 0,01 m y se escriben sólo las celdas húmedas en el array TileDB sparse con compresión ByteShuffle+ZSTD-9. Al finalizar se verifica la integridad mediante muestreo aleatorio.}
\label{fig:etl-flow}
\end{figure}

\begin{enumerate}
\item Abre los 4 ficheros GeoTIFF (H, QX, QY, MH) correspondientes con rasterio.
\item Lee el ráster en franjas de 5 000 filas (CHUNK\_ROWS) usando lectura por ventana (windowed read) para evitar cargar 3 GB/variable en RAM.
\item Aplica la máscara de celdas mojadas: H $\geq$ 0,01 m.
\item Acumula las coordenadas y valores de las celdas mojadas en listas.
\item Realiza una única escritura en TileDB por paso temporal $\rightarrow$ 1 fragmento por paso.
\end{enumerate}

La lectura paralela de las 4 bandas se realiza con \texttt{ThreadPoolExecutor(max\_workers=4)}, reduciendo el tiempo de I/O.

Memoria pico estimada: 4 bandas $\times$ 5 000 filas $\times$ 23 040 cols $\times$ 4 B $\approx$ 1,8 GB.

\FloatBarrier
\section{Gestión de múltiples escenarios}\label{gestion-de-multiples-escenarios-argumento---dataset}

El sistema está diseñado para procesar y almacenar múltiples escenarios de simulación de forma independiente. Cada escenario ocupa su propio array TileDB en disco y se identifica mediante un alias corto (datos1, datos2, \ldots) que \texttt{config.py} traduce al nombre real del directorio de salida de Triton. De este modo el resto del código (ETL, consultas, aplicación web) opera siempre con el alias y no depende de los nombres largos de los ficheros fuente.

El módulo de ingesta acepta el argumento \texttt{-\/-dataset} para seleccionar el escenario a procesar. Dos variables de entorno controlan las rutas:

\begin{itemize}
\item \texttt{TRITON\_BASE\_URI}: directorio donde se crearán los arrays TileDB, bajo el subdirectorio \texttt{triton\_results/}.
\item \texttt{TRITON\_GTIFF\_DIR}: directorio raíz donde están las carpetas de GeoTIFF fuente (\texttt{datos1/}, \texttt{datos2/}, etc.), cada una con los 80 ficheros del escenario correspondiente.
\end{itemize}

Un ejemplo de invocación para el escenario \texttt{datos1} en local:

\begin{verbatim}
TRITON_BASE_URI=/ruta/datos/triton_results \
TRITON_GTIFF_DIR=/ruta/datos \
  python geotiff_to_tiledb_sparse.py --dataset datos1
\end{verbatim}

La estructura de directorios esperada es:

\begin{verbatim}
/ruta/datos/
  triton_results/        <- TRITON_BASE_URI
    output_10_.../       <- arrays TileDB generados
  datos1/                <- TRITON_GTIFF_DIR/datos1
    H_06_00.tif
    QX_06_00.tif  ...
\end{verbatim}

La aplicación web descubre automáticamente los \textit{datasets} disponibles inspeccionando el directorio \texttt{triton\_results/} apuntado por \texttt{TRITON\_BASE\_URI}: cualquier subdirectorio que contenga un array TileDB válido aparece en el selector de la interfaz. La fecha de inicio del escenario se extrae del nombre del directorio fuente (formato \texttt{..\_YYYYMMDD\_YYYYMMDD}), sin necesidad de lectura adicional del array. Añadir un nuevo escenario consiste en ejecutar el ETL con el alias correspondiente; no es necesario modificar ningún fichero de configuración adicional.

